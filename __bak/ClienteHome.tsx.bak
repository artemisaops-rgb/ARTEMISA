import { useAuth } from "@/contexts/Auth";
export default function ClienteHome() {
  const { user } = useAuth();
  return (
    <div className="container-app p-6 space-y-4">
      <h1 className="text-2xl font-bold">Mi perfil</h1>
      <div className="rounded-2xl border bg-white p-4 space-y-2">
        <div className="font-semibold">{user?.displayName || "Usuario"}</div>
        <div className="text-sm text-slate-600">{user?.email}</div>
      </div>
      <section className="rounded-2xl border bg-white p-4">
        <div className="font-semibold mb-1">Mis puntos</div>
        <div className="text-slate-600 text-sm">Próximamente.</div>
      </section>
      <section className="rounded-2xl border bg-white p-4">
        <div className="font-semibold mb-1">Historial</div>
        <div className="text-slate-600 text-sm">Próximamente.</div>
      </section>
    </div>
  );
}
